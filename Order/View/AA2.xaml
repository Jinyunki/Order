<UserControl x:Class="Order.View.AA2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Order.View"
             mc:Ignorable="d" Background="#e9e9"
             d:DesignHeight="250" d:DesignWidth="250"
             DataContext="{Binding Source={StaticResource Locator}, Path=SideBarViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Message,UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="1" Text="Order Count :"/>
        <TextBlock Grid.Row="2" Text="Order Clear :"/>
    </Grid>
</UserControl>
